// Import sprite via Vite so it gets hashed correctly in production build.
// All JS files must use this exported URL instead of hardcoded './img/sprite.svg'
import spriteUrl from '../img/sprite.svg';
export default spriteUrl;
